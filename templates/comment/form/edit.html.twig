{% extends 'base.html.twig' %}

{% block title %}
    {{ app.user.fullname }} - Edit Comment
{% endblock %}

{% block body %}
	<div class="bg-light p-2">
		{{ form_start(form) }}
			{{ form_widget(form) }}
			<button type="submit" class="btn btn-primary">Simpan</button>
		{{ form_end(form) }}
	</div>
{% endblock %}